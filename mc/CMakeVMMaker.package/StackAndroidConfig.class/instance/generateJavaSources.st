java sources
generateJavaSources
	(self buildDir / 'src' / 'org' / 'smalltalk' / 'stack') ensureCreateDirectory. 
	"self generateJavaActivity.
	self generateJavaEvents.
	self generateJavaNetwork.
	self generateJavaVM.
	self generateJavaView.
	"
	
	
	
	"(self buildDir / 'src' / 'smalltalk' / 'eu' / 'android') ensureCreateDirectory."
	"self generateActivity"
	"self generate"