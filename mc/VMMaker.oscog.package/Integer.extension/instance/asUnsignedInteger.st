*VMMaker-interpreter simulator
asUnsignedInteger
	"TODO: (ESTEBAN) I'm commenting this because it does not work with 
	the assert, but translation seems to be working. Of course this is a temporary 
	hack and has to be removed in the near future."
	"self assert: self >= 0."
	^self